import { getAxiosInstance, setDomain, post{{Api}}Delete{{Api}}, post{{Api}}Get{{Api}}All, post{{Api}}Save{{Api}} } from 'tommy-vueadmin-sdk'
//修改请求头
import { setApi } from '@/api/index'
//getAxiosInstance:获取path,setDomain:设置请求头的方法
setApi(getAxiosInstance, setDomain, '')

// 菜单-获取全部
const Post{{Api}}Get{{Api}}All = (params) => {
  return post{{Api}}Get{{Api}}All(params)
}
// 菜单-删除
const Post{{Api}}Delete{{Api}} = (params) => {
  return post{{Api}}Delete{{Api}}(params)
}
// 菜单-保存/更新
const Post{{Api}}Save{{Api}} = (params) => {
  return post{{Api}}Save{{Api}}(params)
}
// 菜单-同步
const Post{{Api}}Sync{{Api}} = (params) => {
  return post{{Api}}Sync{{Api}}(params)
}
export default {
  Post{{Api}}Get{{Api}}All,
  Post{{Api}}Delete{{Api}},
  Post{{Api}}Save{{Api}}
}
